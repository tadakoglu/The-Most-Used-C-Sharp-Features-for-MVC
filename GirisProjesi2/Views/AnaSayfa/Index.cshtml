@model IEnumerable<string>
@{
    ViewData["Title"] = "Index";
    Layout = null;
}
<a href="AnaSayfa/BoyutHesapla">
    pau.edu.tr Sayfa Boyutu Hesapla/Asenkron İstek
</a>

<h3>Bu örnek projede ASP.NET CORE MVC'de sık kullanılan birçoğu yeni C# özellikleriyle staj projesine hazırlık olması açısından pratik yapıyorum.</h3>
<ul>
    @foreach (string str in Model)
    {
        <li>@str</li>
    }
</ul>
@if (Model.Count() == 0)
{
    <h4>İstediğiniz filtrelerdeki gibi bir ürün bulunmamaktadır</h4>
}

